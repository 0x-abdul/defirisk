# Keep shell scripts and git hooks LF-terminated so they stay executable
# regardless of the checkout platform (CRLF breaks the shebang line).
scripts/git-hooks/* text eol=lf
*.sh text eol=lf
data/api/** text eol=lf
