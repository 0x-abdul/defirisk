---
// Full protocol directory: for now just redirect to the homepage which already
// shows the full directory via ProtocolListView. A standalone /protocols/ route
// is unnecessary at v1 since the homepage IS the protocol directory.
return Astro.redirect('/');
---
